[pytest]
# Ensure the project root is importable so `import satellite_orbit_simulator`
# works without installing the package.
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v
